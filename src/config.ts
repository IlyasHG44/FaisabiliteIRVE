// Drapeaux de fonctionnalités — passer à true pour activer en production.
export const FEATURES = {
  // Bornes de recharge à proximité (concurrence / marché).
  // Codé et fonctionnel, gardé "en off" pour l'instant : activer quand on le décidera.
  bornes: false,
};
